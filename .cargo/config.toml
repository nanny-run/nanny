# Statically link the MSVC CRT on Windows so the binary has no external
# runtime dependencies (no VCRUNTIME140.dll required on the target machine).
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "target-feature=+crt-static"]
